import { SlideMenuBackDirective } from './slide-menu-back.directive';

describe('SlideMenuBackDirective', () => {
  it('should create an instance', () => {
    const directive = new SlideMenuBackDirective();
    expect(directive).toBeTruthy();
  });
});
